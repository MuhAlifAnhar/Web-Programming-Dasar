const student = ['alif', 'anhar', 'muh'];
const guru = ['alif', 'anhar', 'muh'];

const sekolah = student.concat(guru);
sekolah.sort();

sekolah.reverse();

sekolah.push('jokow');

console.log(sekolah);